<odoo>
    <record id="view_sedar_job_order_tree" model="ir.ui.view">
        <field name="name">sedar.job.order.tree</field>
        <field name="model">sedar.job.order</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="customer_id"/>
                <field name="vessel_id"/>
                <field name="requested_date"/>
                <field name="state"/>
            </tree>
        </field>
    </record>

    <record id="view_sedar_job_order_form" model="ir.ui.view">
        <field name="name">sedar.job.order.form</field>
        <field name="model">sedar.job.order</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_dispatch" string="Dispatch" type="object" invisible="state != 'requested'"/>
                    <button name="action_start" string="Start" type="object" invisible="state != 'dispatched'"/>
                    <button name="action_complete" string="Complete" type="object" invisible="state != 'in_progress'"/>
                    <field name="state" widget="statusbar"/>
                </header>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="customer_id"/>
                        <field name="vessel_id"/>
                        <field name="origin_port"/>
                        <field name="destination_port"/>
                        <field name="requested_date"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_sedar_job_order" model="ir.actions.act_window">
        <field name="name">Job Orders / Dispatch</field>
        <field name="res_model">sedar.job.order</field>
        <field name="view_mode">tree,form</field>
    </record>
</odoo>
